# Database configuration
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:hr_management}?user=${DB_USERNAME:hr_user}&password=${DB_PASSWORD:hr_password}
spring.datasource.driver-class-name=org.postgresql.Driver

# Server configuration
server.port=${SERVER_PORT:8080}